import os

def read_bak():
    with open("scratch/TemplateBuilder.tsx.bak", "r") as f:
        return f.read()

# I will just write the files manually because writing a reliable parser in Python for JSX is hard.
